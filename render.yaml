services:
  - type: web
    name: ai-ops-command-center
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn commercial_app:app
    autoDeploy: true
    healthCheckPath: /api/health
